# Server-only (set in Vercel/hosting dashboard — never in frontend)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password
SMTP_FROM="YoSite Studio <noreply@yourdomain.com>"
CONTACT_NOTIFY_EMAIL=yosefokra2050@gmail.com

# Optional: override API URL in local dev (default: /api/contact)
# VITE_CONTACT_API_URL=/api/contact
